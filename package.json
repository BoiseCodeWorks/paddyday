
  {
    "name": "paddyday",
    "version": "1.0.0",
    "scripts": {
      "build": "npm run setup:client && npm run setup:server && npm run build:client",
      "setup:client": "cd paddyday.client && npm i",
      "setup:server": "cd paddyday.server && npm i",
      "build:client": "npm run --prefix paddyday.client build",
      "start": "node paddyday.server/index.js"
    },
    "engines": {
      "node": "^14.15.4"
    }
  }
  